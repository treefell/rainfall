#include <stdio.h>
#include <stdlib.h>

void	p(char *str)
{
	printf(str);
	return ;
}

void	n(void)
{
	char	buff[520];

	fgets(buff, 512, stdin);
	p(buff);
	if (m == 0x1025544)
		system("/bin/cat /home/user/level5/.pass");
	return ;
}

int		main(void)
{
	n();
	return (0);
}
